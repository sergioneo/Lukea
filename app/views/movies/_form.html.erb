<%= form_for(@movie, html: {multipart: true}) do |f| %>
  <% if @movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% @movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br>
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  <div class="field">
    <%= f.label :pelicula_url %><br>
    <%= f.text_field :pelicula_url %>
  </div>
  
  <span class="image">
    <%= f.file_field :image, accept: 'image/jpeg,image/gif,image/png' %>
  </span>
  
  <div class="field">
    <%= f.label :category %><br>
    <%= collection_check_boxes(:movie, :category_ids, Category.order("category"), :id, :category, {:selected => @movie.category_ids, :include_blank => true}, {:multiple => true}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
